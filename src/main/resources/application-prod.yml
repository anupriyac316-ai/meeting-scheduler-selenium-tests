spring:
  datasource:
    url: jdbc:mysql://meeting-scheduler-db.cp2kgqmcmjyp.ap-south-1.rds.amazonaws.com:3306/meeting_scheduler?useSSL=true&requireSSL=true&serverTimezone=UTC
    username: admin
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true